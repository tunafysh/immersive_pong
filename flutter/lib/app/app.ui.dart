import 'package:flutter/material.dart';


class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext ctx) {
    return MaterialApp(
      title: "Immersive Pong",
      debugShowCheckedModeBanner: false,      
      home: Text("In development.")
    );
  }
}

